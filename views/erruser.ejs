<!DOCTYPE html>
<html>
<head>
    <title>JEdrock - ошибка</title>
    <link rel="stylesheet" href="assets/css/bootstrap.min.css">
    <link rel="stylesheet" href="assets/css/style.css">
    <link
    href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100;200;300;400;500;600;700;800;900&display=swap"
    rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/js-sha256@0.9.0"></script>
</head>
<body>
    <div class="error-block">
        <h1 class="err-h">Произошла ошибка!</h1>
        <p class="err-p">Вас нет в базе данных.</p>
        <p class="err-p">Требуется покупка проходки.</p>

        <div class="btns-err">
            <button class="btn-login-main" onclick="buy()">Купить</button>
            <button class="btn-login-main" onclick="oAuth()">Войти</button>
            <script>
                function buy() {
                    function getFormSignature(account, currency, desc, sum, secretKey) {
                        var account = 'testAccount';
                        var currency = 'USD';
                        var desc = 'testDescription';
                        var sum = '100';
                        var secretKey = 'testKey';
                        var hashStr = account + '{up}' + currency + '{up}' + desc + '{up}' + sum + '{up}' + secretKey;
                        var hash = sha256(hashStr);
                    }

                }

                function oAuth(){
                    location.replace("https://discord.com/api/oauth2/authorize?client_id=1095721219275358330&redirect_uri=http%3A%2F%2F127.0.0.1%3A5000%2F&response_type=code&scope=identify")
                }
            </script>
        </div>
    </div>

<script src="assets/js/bootstrap.min.js"></script>
<script src="assets/script.js"></script>
</body>
</html>
